<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invoice_rtl.list">
      <field name="name">invoice_rtl list</field>
      <field name="model">invoice_rtl.invoice_rtl</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invoice_rtl.action_window">
      <field name="name">invoice_rtl window</field>
      <field name="res_model">invoice_rtl.invoice_rtl</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invoice_rtl.action_server">
      <field name="name">invoice_rtl server</field>
      <field name="model_id" ref="model_invoice_rtl_invoice_rtl"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invoice_rtl" id="invoice_rtl.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invoice_rtl.menu_1" parent="invoice_rtl.menu_root"/>
    <menuitem name="Menu 2" id="invoice_rtl.menu_2" parent="invoice_rtl.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invoice_rtl.menu_1_list" parent="invoice_rtl.menu_1"
              action="invoice_rtl.action_window"/>
    <menuitem name="Server to list" id="invoice_rtl" parent="invoice_rtl.menu_2"
              action="invoice_rtl.action_server"/>
    -->
  </data>
</odoo>